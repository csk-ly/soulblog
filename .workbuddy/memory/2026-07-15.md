# 2026-07-15 工作日志

## 修复 UI 三个问题

### 1. card 背景太深
- 文件：`src/styles/globals.css`
- `.card` 背景从 `rgba(255,255,255,0.12)` 改为 `0.06`，边框 alpha 从 `0.18` 改为 `0.12`，让磨砂卡片更通透

### 2. 导航默认选中状态错误
- 文件：`src/components/nav-card.tsx`
- `hoveredIndex` 初始值 `0` → `-1`，默认无选中
- 删除 icons 模式下自动 setHoveredIndex(activeIndex) 的 useEffect
- 新增 pathname 变化时重置 hoveredIndex 为 -1 的 useEffect（避免跳转后残留高亮）
- motion.div 高亮背景的 style 增加 `opacity: hoveredIndex < 0 ? 0 : 1`

### 3. 文章列表时间手机端换行
- 文件：`src/app/blog/page.tsx`
- 日期 span 从 `w-[44px]` → `w-[52px]`，并加 `whitespace-nowrap` 防止换行

### 4. 修复"点击文章后导航按钮应选中"（用户反馈）
- 同一文件 `src/components/nav-card.tsx`
- `activeIndex` 改为前缀匹配：`pathname === item.href || pathname.startsWith(item.href + '/')`，让 `/blog/[id]` 详情页也激活"近期文章"
- 加 `if (!pathname) return undefined` null 检查，防止 SSR 报错
- 引入 `displayedIndex = hoveredIndex >= 0 ? hoveredIndex : activeIndex ?? -1`，统一高亮判定
- `motion.div` 高亮背景、图标 active 状态、文本高亮都改用 `displayedIndex`
- `layoutId` 从固定 `'nav-hover'` 改为 `nav-hover-${form}`，防止 full↔icons 表单切换时 framer-motion 共享布局动画错位
- 给 Link 加 `onMouseLeave={() => setHoveredIndex(-1)}`，桌面端 hover 离开后回落到路径激活
- Playwright 截图验证：/blog 列表页和 /blog/[id] 详情页均正确显示"近期文章"选中（青色填充+背景高亮），首页无选中

## 环境问题
- 本地 pnpm v11.5.2 与 lockfile 不兼容，node_modules 残缺
- 用 `CI=true pnpm install --no-frozen-lockfile` 重装修复
- `pnpm dev` 启动报 `NODE_OPTIONS=--use-system-ca` 不允许，需 `env -u NODE_OPTIONS node node_modules/next/dist/bin/next dev` 绕过
- dev 服务器在端口 2025 正常运行

## 新增名言卡片（quoteCard）
- 数据文件：`public/quotes.json`（10 条中英文名言，含 text + author 字段）
- 组件文件：`src/app/(home)/quote-card.tsx`
  - 'use client'，fetch 加载 quotes.json 后随机打乱
  - 状态机：typing（80-120ms/字）→ 暂停 3s → erasing（30ms/字）→ 下一条
  - 闪烁光标用 Tailwind animate-pulse，引用符用 brand 色半透明
  - 作者信息仅在打字完成后才显示（更自然）
  - 卡片宽 280、高 120、order 9
- 配置：在 `card-styles.json` 添加 quoteCard，默认启用
- 注册：`src/app/(home)/page.tsx` 引入并条件渲染
- Playwright 截图验证：打字/暂停/擦除 三个阶段均正常

### 修复 quoteCard 排版重叠（用户反馈）
- 初始位置与 articleCard 重叠，移到 articleCard 正下方
- 引用 `hiCardStyles`、`socialButtonsStyles` 复刻 articleCard 的 x 公式
- x = `center.x + hiCardStyles.width/2 - socialButtonsStyles.width - CARD_SPACING - articleCardStyles.width`
- y = `center.y + hiCardStyles.height/2 + CARD_SPACING + articleCardStyles.height + CARD_SPACING`
- 截图确认：左下角形成 article（上）+ quote（下）的纵向布局，不再重叠

### 第三次修复 quoteCard 排版（用户反馈"超出了一屏"+"手机端名言没了"）
- 问题1：quote card 底部 900px 超出 832px 视口高度
- 问题2：page.tsx 中 quote card 渲染条件为 `!maxSM`，手机端被隐藏
- 解决：
  - 尺寸从 220×90 → **266×76**（与 article card 同宽，压缩高度避免溢出）
  - y 间距从 CARD_SPACING+24 → **+6**（紧贴 article 下方，底部 814 < 832）
  - 内部样式适配 76px：`p-3`、`text-xs`、`text-[10px]`（作者）
  - 渲染条件从 `!maxSM &&` → **无条件渲染**（手机端也显示）
- Playwright 验证：桌面 1280×832 底部 814 不溢出；手机端 375×812 也正常显示

### 第五次调整：quote 与 beian 起始 Y 对齐（用户反馈）
- 用户要求：名言卡片和右侧备案卡片位置起始一样（同 Y）
- beian y = `center.y + hiCardStyles.height/2 + CARD_SPACING + 180`（=752）
- quote y 改为相同公式：`center.y + hiCardStyles.height/2 + CARD_SPACING + shareCardStyles.height`（shareCardStyles.height=180，同样=752）
- 验证：两者 y 均为 752，对齐成功
- 用户要求：
  1. quote 卡片底部与"随机推荐"（shareCard）底部对齐
  2. article 卡片（最新文章）往上去一点
  3. quote 与 article 间隔大一点
- 修改：
  - `aritcle-card.tsx` y 计算减去 12：`center.y + hiCardStyles.height/2 + CARD_SPACING - 12`（article 上移 12px）
  - `quote-card.tsx` 引入 `shareCardStyles`，y 改为对齐 share 底部：
    `center.y + hiCardStyles.height/2 + CARD_SPACING + socialButtonsStyles.height + CARD_SPACING + shareCardStyles.height - styles.height`
- 验证：article y=560（-12）、share bottom=836、quote bottom=836（对齐），间距 40px
- 截图确认：左下形成 article(560)→quote(760)→share 同底 的整齐布局
